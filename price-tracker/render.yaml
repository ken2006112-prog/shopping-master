services:
  - type: web
    name: price-tracker
    env: docker
    plan: free
    branch: master
    numInstances: 1
    healthCheckPath: /api/products
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: sqlite-data
      mountPath: /app/db
      sizeGB: 1
